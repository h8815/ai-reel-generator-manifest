apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-reel-generator-charts.fullname" . }}
  labels:
    {{- include "ai-reel-generator-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ai-reel-generator-charts.selectorLabels" . | nindent 4 }}
