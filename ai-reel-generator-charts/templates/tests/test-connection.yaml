apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ai-reel-generator-charts.fullname" . }}-test-connection"
  labels:
    {{- include "ai-reel-generator-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ai-reel-generator-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
